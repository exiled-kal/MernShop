import './Navbar.css';

const Navbar = () => {
  return (
    <nav className="navbar">
      {/* logo */}
      .nab
      {/* links */}
      {/* hamburger menu */}
    </nav>
  );
};

export default Navbar;
